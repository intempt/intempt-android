# Intentionally empty. The library ships its own rules via consumerProguardFiles, and this
# module exists partly to prove that. Adding keep rules here would mask a missing consumer
# rule in the AAR, which is the failure this build is meant to catch.
